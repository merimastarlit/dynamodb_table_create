# dynamodb_table_create

## in this file, we created DynamoDB (DDB) table and used it with CRUD, which stands for create, read, update and delete. That way,w e can influence on the DDB table using python codes.
